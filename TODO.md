# TODO

- [ ] Match arm alignment — preserve intentional column alignment in match arms
      (e.g. `(true,true)   =>` aligned with `(false,false) =>`)
- [ ] Import sorting within groups — alphabetical sorting of imports within each
      group needs review and refinement
