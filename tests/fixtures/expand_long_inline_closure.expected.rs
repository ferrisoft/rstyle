fn f() {
    let x = items
        .map(|n| {
            let squared = n * n;
            let offset = squared + 42 + 98989;
            let offset = squared + 42 + 98989;
            let offset = 5;
            offset
        });
}
